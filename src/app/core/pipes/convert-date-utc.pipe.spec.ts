import { ConvertDateUTCPipe } from './convert-date-utc.pipe';

describe('ConvertDateUTCPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDateUTCPipe();
    expect(pipe).toBeTruthy();
  });
});
